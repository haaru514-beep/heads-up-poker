services:
  - type: web
    name: heads-up-poker
    env: docker
    autoDeploy: true
    disk:
      name: poker-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DATA_DIR
        value: /data
